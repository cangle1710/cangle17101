[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
timeout = 30
filterwarnings =
    ignore::DeprecationWarning
markers =
    throughput: performance / latency tests (slower)
    e2e: end-to-end tests
